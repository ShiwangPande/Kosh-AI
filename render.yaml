services:
  # ── Backend API ──────────────────────────────────────────
  - type: web
    name: kosh-ai-backend
    env: docker
    dockerfilePath: infrastructure/Dockerfile.backend
    plan: free # or your desired plan
    envVars:
      - key: APP_ENV
        value: production
      - key: DEBUG
        value: "false"
      - fromGroup: kosh-ai-secrets

  # ── Background Worker ────────────────────────────────────
  - type: worker
    name: kosh-ai-worker
    env: docker
    dockerfilePath: infrastructure/Dockerfile.worker
    plan: free
    envVars:
      - key: APP_ENV
        value: production
      - fromGroup: kosh-ai-secrets

envVarGroups:
  - name: kosh-ai-secrets
    envVars:
      - key: DATABASE_URL
        value: placeholder
      - key: DATABASE_URL_SYNC
        value: placeholder
      - key: REDIS_URL
        value: placeholder
      - key: CELERY_BROKER_URL
        value: placeholder
      - key: CELERY_RESULT_BACKEND
        value: placeholder
      - key: JWT_SECRET_KEY
        value: placeholder
      - key: SECRET_KEY
        value: placeholder
      - key: CLOUDINARY_CLOUD_NAME
        value: placeholder
      - key: CLOUDINARY_API_KEY
        value: placeholder
      - key: CLOUDINARY_API_SECRET
        value: placeholder
      - key: BACKEND_CORS_ORIGINS
        value: placeholder
